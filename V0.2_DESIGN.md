# 股票研究 Agent：V0.2 设计

**目标：** 从本地合成样例升级为“真实资料 + 可替换模型 + 可持久化任务”的可用研究原型。

## 1. V0.2 一句话目标

用户输入一家港股公司和研究问题，系统从真实公开资料中完成一次研究，使用可替换的大模型生成定性分析，保存任务和报告，并支持再次打开和追问。

## 2. 相比 V0.1 的变化

| 能力 | V0.1 | V0.2 |
|---|---|---|
| 数据 | 本地合成样例 | 真实 HKEX/公司 IR 文档 |
| 模型 | 规则化分析 | 可替换 LLM Adapter |
| 存储 | 内存 | SQLite/PostgreSQL 持久化 |
| 任务 | 本地同步执行 | 可查询状态、可恢复 |
| 报告 | CLI 输出 | 保存报告版本并支持追问 |
| 事实 | 规则抽取 | 规则抽取 + LLM 辅助候选抽取 + 审计 |
| 交互 | CLI | CLI/API，保留后续 Web 接口 |

## 3. V0.2 范围

### P0：必须完成

1. 接入真实公开文档，优先采用用户指定 URL、HKEXnews 或公司 IR；
2. 保存原始文档、内容哈希、发布时间和解析状态；
3. 将文档切成带页码/行号的证据片段；
4. 将事实、计算、结论和证据持久化；
5. 接入统一 LLM Adapter，至少支持一种模型；
6. Context Builder 注入截止日期、用户偏好、历史研究和证据；
7. 使用 LLM 完成业务、行业和风险分析；
8. 使用代码完成财务指标和估值；
9. Reviewer 检查引用、日期、数字和结构化输出；
10. 报告可保存、读取和追问；
11. 任务失败后可以从步骤状态恢复。

### P1：本版本可选

- 最小 FastAPI 接口；
- 任务状态查询；
- SSE/轮询进度；
- 简单的报告 HTML 页面；
- 用户明确反馈写入偏好；
- 增量更新的基础版本。

## 4. 不在 V0.2 做

- 全港股批量覆盖；
- 实时行情和交易；
- 扫描版 PDF OCR；
- 自动微调或修改模型；
- 多用户权限系统；
- 复杂 Agent Teams；
- 生产级数据再分发；
- 个性化买卖建议。

## 5. 推荐架构

```text
CLI / API
  ↓
Research Service
  ↓
Persistent Orchestrator
  ├── Context Builder
  ├── Planner
  ├── Document Worker
  ├── Fact Worker
  ├── LLM Analysis Worker
  ├── Calculation Worker
  ├── Reviewer
  └── Report Compiler
  ↓
Provider Seams
  ├── DocumentProvider
  ├── MarketDataProvider
  ├── LLMProvider
  └── StorageProvider
  ↓
SQLite/PostgreSQL + 文件存储
```

先使用 SQLite 验证持久化接口，部署到服务环境时切换 PostgreSQL。核心领域对象不依赖具体数据库。

## 6. 真实数据接入策略

### 6.1 文档

首选顺序：

```text
HKEXnews / 公司 IR
  > 用户提供的公开文档 URL
  > 可靠新闻源
```

第一阶段先支持用户提供 URL 和已知文档列表，避免把不稳定的全站爬取作为核心依赖。

### 6.2 行情

继续保留行情 Provider 接口。原型使用 Yahoo EOD；真实部署时替换为有授权的港股数据供应商。

### 6.3 数据质量

所有数据必须保留：

- 来源；
- 发布时间；
- 数据所属期间；
- 币种和单位；
- 内容哈希；
- 解析状态；
- 数据源 Provider。

## 7. LLM Adapter

定义统一接口：

```python
class LLMProvider(Protocol):
    def complete(self, request: ModelRequest) -> ModelResponse: ...
```

请求必须记录：

- provider；
- model；
- prompt/version；
- 输入上下文摘要；
- 输出 Schema；
- token 和耗时（供应商支持时）。

模型输出必须经过 JSON Schema 校验。模型负责：

- 业务理解；
- 行业和竞争分析；
- 风险识别；
- 证据解释。

模型不负责最终财务数学。

## 8. V0.2 研究流程

```text
1. 创建/读取 ResearchProject
2. 创建 ResearchRun 和截止日期
3. 获取真实文档并去重
4. 解析文档并建立 EvidenceChunk
5. 抽取 FactCandidate
6. Reviewer 验证事实
7. LLM 生成业务、行业和风险 Claims
8. 代码计算指标和三情景 DCF
9. Reviewer 检查全部 Claims
10. 持久化 Report
11. 允许用户追问并复用当前上下文
```

## 9. 持久化要求

将 V0.1 的内存对象映射到数据库：

```text
companies
research_projects
research_runs
research_steps
documents
evidence_chunks
facts
calculations
claims
reports
user_preferences
run_events
```

任务状态更新和事件写入必须在同一事务中完成，避免状态已经完成但事件没有记录。

## 10. 追问和上下文策略

追问只读取必要上下文：

```text
当前问题
+ 当前报告相关章节
+ 相关 Facts/Calculations
+ 相关 Evidence
+ 用户偏好
```

不要把整份历史文档和所有对话重新塞入模型。

追问类型：

- 解释结论；
- 查看证据；
- 寻找反方；
- 修改估值假设；
- 请求更新资料。

## 11. V0.2 验收标准

使用一家公司真实公开文档完成任务后：

1. 文档可下载、去重并保存来源；
2. 至少抽取 5 个结构化财务事实；
3. 每个事实关联证据片段；
4. 至少计算 CAGR、利润率、自由现金流、净现金和 DCF；
5. LLM 生成业务和风险结论；
6. 关键结论有引用或计算依据；
7. 截止日期之后的资料不会进入结果；
8. 任务状态和报告重启后仍可读取；
9. 追问能复用已有报告上下文；
10. 模拟模型或数据源失败后，任务能够重试或明确进入失败状态。

## 12. 迭代顺序

### V0.2-A：持久化

先把项目、运行、步骤、事件、文档和报告保存到 SQLite。

### V0.2-B：真实文档

接入一份真实港股年报或公告，完成 PDF 到 EvidenceChunk 的链路。

### V0.2-C：模型分析

接入一个 LLM Provider，输出带证据 ID 的业务和风险 Claims。

### V0.2-D：服务化

增加最小 API、任务状态查询和报告读取。

### V0.2-E：质量和追问

增加 Reviewer、失败恢复、报告追问和第一批真实评测样例。

