# 第一版可验收需求（MVP）

## 目标

完成一个不依赖外部模型 Key 或付费数据库、可以本地运行的单公司港股研究垂直切片：

```text
输入公司和研究问题
→ 创建研究任务
→ 生成固定研究计划
→ 读取本地/已下载文档
→ 抽取财务事实并关联证据
→ 计算基础指标和 DCF 情景估值
→ 执行规则化审计
→ 输出 JSON/Markdown 研究报告
```

**实现状态：** 第一版本地垂直切片和本地 Web/SQLite 持久化已完成；真实 HKEX 数据连接、异步长任务和生产部署属于下一阶段。

## 必须完成

### R1：研究任务

- 通过 Python API 或 CLI 创建研究任务；
- 必须携带公司、问题和 `as_of_date`；
- 任务有明确状态和步骤；
- 任务运行结果可序列化。

### R2：资料和证据

- 支持本地文本/HTML 文档；
- 支持 PDF 文本提取；
- 每份文档保存来源 URL、发布时间和内容哈希；
- 证据片段保留页码或行号。

### R3：财务事实

- 支持收入、净利润、经营现金流、现金、负债、资本开支；
- 事实包含期间、单位、币种、来源和置信度；
- 多年度列头不明确时不得猜测。

### R4：计算

- 计算收入 CAGR；
- 计算利润率和现金流率；
- 计算净现金/净负债；
- 支持 DCF 基础、乐观、悲观三种情景；
- 输入、公式版本和结果都可查看。

### R5：报告

- 输出公司研究摘要；
- 输出财务指标表；
- 输出估值情景；
- 输出证据引用；
- 输出风险、未解决问题和免责声明；
- 报告中的结论必须关联证据或计算。

### R6：审计和可靠性

- 检查截止日期；
- 检查缺失来源；
- 检查低置信度事实；
- 检查计算输入完整性；
- 步骤失败时保留错误和中间结果；
- 所有核心逻辑有自动化测试。

### R7：运行方式

- `PYTHONPATH=src python3 -m stock_research --demo` 可运行完整样例；
- `PYTHONPATH=src python3 -m unittest discover -s tests -v` 全部通过；
- 不要求安装外部 Python 包；
- 外部数据连接器和模型调用保持可替换。

## 第一版明确不做

- 完整 Web UI（当前仅有本地 MVP 页面）；
- 用户登录和多租户；
- 实时行情和交易；
- 全港股覆盖；
- OCR 扫描件；
- 自动训练或修改模型；
- 个性化买卖建议；
- 生产级数据授权和商业再分发。

## 完成定义

运行 Demo 后，能够得到一份真实结构完整的研究报告，且报告中的财务数字、计算结果和证据位置都可以从代码输出中复核。

当前验收入口：

```bash
PYTHONPATH=src python3 -m stock_research --demo
PYTHONPATH=src python3 -m unittest discover -s tests -v
```
