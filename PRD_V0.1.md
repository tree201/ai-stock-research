# 股票研究 Agent 平台：第一版产品需求

**版本：** v0.1

**状态：** 设计中

**目标：** 用最小但完整的端到端流程，验证“可验证的长期股票研究”是否成立。

## 1. 产品定义

### 1.1 一句话定位

面向认真个人投资者和独立研究员的专业级股票研究工作台，帮助用户完成可验证、可复算、可持续更新的公司研究。

### 1.2 第一版核心承诺

给定一家港股公司、一个研究问题和一个截止日期，系统能够生成一份带证据、可复算估值、风险和反方证据的研究报告，并支持任务中断恢复和基于报告的追问。

### 1.3 非目标

第一版不负责：

- 预测短期股价或承诺收益率；
- 自动下单或执行交易；
- 提供个性化买卖建议；
- 覆盖所有市场和所有股票；
- 替代专业投研机构的数据授权体系；
- 让 Agent 自动修改模型、代码或金融计算规则。

## 2. 目标用户和使用场景

### 2.1 目标用户

- 有一定投资基础的个人投资者；
- 学生或独立研究员；
- 长期跟踪少量港股公司的用户。

用户希望快速理解公司，并且能够查看证据、追问假设、保存研究历史和跟踪投资逻辑变化。

### 2.2 核心场景

#### 场景 A：首次深度研究

用户输入：

> 截至 2025 年底，研究腾讯是否适合长期持有，重点分析游戏、广告、云业务、AI 投资和估值。

系统生成研究计划，执行资料收集、财务分析、估值和审计，最后生成报告。

#### 场景 B：报告追问

用户在报告中追问：

- 这个结论的证据是什么？
- 为什么认为现金流质量良好？
- 如果云业务增速下降到 15%，估值如何变化？
- 你有没有寻找反方证据？

系统基于当前报告、证据和模型回答，不从零开始研究。

#### 场景 C：增量更新

新财报或重大公告出现后，用户要求：

> 更新上次腾讯的研究报告，只分析新信息和投资逻辑变化。

系统对比历史研究，输出新增事实、假设变化、估值变化和结论变化。

## 3. 产品原则

1. **聊天是入口，研究项目是核心对象。**
2. **前台简单，后台可审计。** 用户不需要理解 Agent 编排。
3. **模型负责理解和解释，代码负责数据和数学。**
4. **每个重要结论都应有证据。**
5. **默认不打断用户。** 只有在信息缺失会实质改变结果时才追问。
6. **长期研究优先于一次性研报。**
7. **所有自适应变化可查看、可撤销、可回放。**

## 4. 第一版产品范围

### 4.1 市场和覆盖范围

- 首先支持港股；
- 选择 10～20 家代表性公司做数据和端到端验证；
- 以公司基本面、财务和估值为主；
- 研究截止日期必须是任务的一等属性。

### 4.2 第一版研究维度

- 公司和业务模式；
- 收入和利润结构；
- 财务趋势和现金流质量；
- 行业和主要竞争者；
- 管理层指引和历史兑现情况；
- 估值和情景分析；
- 主要风险和反方证据；
- 后续需要跟踪的指标。

## 5. 用户体验和页面

第一版采用“单研究项目 + 单聊天流 + 可展开报告”的交互，不要求用户配置 Agent。

### 5.1 研究项目首页

展示：

- 新建研究入口；
- 进行中的任务；
- 历史研究项目；
- 最近更新和失败状态。

### 5.2 研究工作区

主要区域包含：

- 聊天输入区；
- 研究计划卡片；
- 任务进度；
- 阶段性结论；
- 最终报告入口。

用户可使用快捷操作：

```text
[开始研究] [查看证据] [解释原因] [寻找反方] [更新报告]
```

### 5.3 报告详情页

报告包含：

- 核心结论；
- 业务分析；
- 财务分析；
- 估值区间；
- Bull/Base/Bear 情景；
- 风险和反方证据；
- 未解决问题；
- 后续跟踪指标。

点击关键结论或数字时，显示：

- 来源文件；
- 原文片段；
- 页码或段落；
- 发布时间和数据期间；
- 计算公式；
- 相关反方证据。

## 6. 核心功能需求

### FR-01 创建研究任务

用户可以输入公司、研究问题和截止日期。

系统应自动识别：

- 证券和公司；
- 研究类型（快速问答或深度研究）；
- 用户要求的重点；
- 默认研究范围。

验收标准：输入一句自然语言问题即可创建任务，不要求用户填写复杂表单。

### FR-02 研究计划

系统为深度研究生成结构化计划，至少包含：

- 子任务名称；
- 子任务目的；
- 所需数据源；
- 依赖关系；
- 预计状态。

用户可以增加、删除或修改重点，但不需要选择具体 Agent。

验收标准：计划可保存、可修改，并且后续执行严格引用当前计划版本。

### FR-03 长任务执行

系统支持：

- 异步执行；
- 任务进度；
- 单步骤超时；
- 失败重试；
- 暂停和取消；
- 从最近 checkpoint 恢复；
- 页面关闭后继续运行。

验收标准：模拟某个数据源失败和服务重启后，任务能够从已完成步骤继续，不重复产生重复数据。

### FR-04 数据和证据获取

第一版优先使用：

- HKEXnews 和公司 IR 作为披露事实来源；
- 一个结构化行情/财务数据源作为原型数据源；
- 官方宏观或行业数据作为补充来源。

每条事实必须保留：

```text
company_id
metric
value
currency
unit
period_start
period_end
published_at
source_url
source_document
source_location
accounting_basis
```

### FR-05 财务抽取和标准化

系统应能从财报中抽取常用数据：

- 收入；
- 毛利和营业利润；
- 净利润；
- 经营现金流；
- 自由现金流；
- 现金和负债；
- 资本开支；
- 股份数；
- 主要分部指标。

抽取结果必须包含原文位置和置信度。低置信度结果进入审查队列，不应静默写入最终结论。

### FR-06 指标和估值计算

代码负责计算：

- 同比和 CAGR；
- 毛利率、净利率；
- 经营现金流率和自由现金流率；
- ROE、ROIC；
- 净现金/净负债；
- PE、PB、PS、EV/EBITDA；
- DCF 和三情景估值。

模型只负责解释假设和结果。用户修改假设后，系统应重新计算并标记报告版本变化。

### FR-07 报告生成

报告必须包含：

- 截止日期；
- 数据源说明；
- 核心结论；
- 关键支持证据；
- 关键反方证据；
- 估值假设；
- 不确定性和未解决问题；
- 后续跟踪指标。

禁止把没有证据的推测写成确定事实。

### FR-08 报告追问

追问默认绑定当前研究项目和报告版本。

系统应区分：

- 已有报告中的事实；
- 新检索到的事实；
- 基于模型假设的推演；
- 无法确认的内容。

### FR-09 用户偏好记忆

用户可以通过自然语言表达偏好，例如：

> 以后少讲市场空间，多分析现金流。

系统将明确反馈保存为偏好，并在下一次构建上下文时注入。

第一版只支持：

- 关注指标；
- 估值偏好；
- 报告风格；
- 证据优先级；
- 是否默认展示反方证据。

系统推测出的偏好必须等待用户确认，不能直接永久生效。

### FR-10 增量更新

系统可以对比当前报告和新信息，输出：

- 新增事实；
- 被推翻或削弱的假设；
- 财务指标变化；
- 估值变化；
- 风险变化；
- 是否需要修改总体结论。

## 7. Agent 和服务边界

第一版不追求大量 Agent，采用少量职责明确的模块：

```text
Request Router
  → Context Builder
  → Planner
  → Research Workers
  → Financial Calculator
  → Reviewer
  → Report Compiler
  → Feedback/Memory Writer
```

### Agent 负责

- 意图识别；
- 研究计划；
- 文档理解；
- 业务和风险分析；
- 证据解释；
- 报告写作。

### 代码和服务负责

- 数据获取；
- 数据标准化；
- 财务计算；
- 日期过滤；
- 状态管理；
- checkpoint 和重试；
- 引用和格式校验。

## 8. 任务状态和持久化

### 8.1 任务状态

```text
created
→ planned
→ collecting_data
→ extracting_facts
→ analyzing
→ calculating
→ reviewing
→ completed
```

允许从任何中间状态进入：

- `paused`；
- `canceled`；
- `failed`；
- `resuming`。

### 8.2 事件记录

至少记录：

- 任务创建；
- 计划版本；
- 数据源调用；
- 文档获取；
- 抽取结果；
- 计算结果；
- Agent 输出；
- 审计结果；
- 用户反馈；
- 报告版本。

研究过程应能通过事件记录重放。

## 9. 质量和评测

### 9.1 基础评测

使用公开金融 QA 数据集测试：

- 财报问答；
- 表格理解；
- 财务计算；
- 引用能力。

### 9.2 自建评测

构建带截止日期的港股研究样例，测试：

- 数字准确率；
- 计算准确率；
- 引用支持率；
- 时间泄漏率；
- 关键风险遗漏率；
- 报告内部数字一致性。

### 9.3 可靠性评测

- 任务完成率；
- 失败重试成功率；
- 中断恢复成功率；
- 相同输入下的结果稳定性；
- 平均耗时和模型成本。

## 10. 非功能需求

### 10.1 可靠性

- 所有长任务可查询状态；
- 页面关闭不会丢失任务；
- 工具调用有超时和重试；
- 任务失败必须给出原因；
- 中间结果不会因为最终报告失败而丢失。

### 10.2 可追溯性

- 关键事实都有来源；
- 报告标记数据截止日期；
- 计算结果可复算；
- Agent、Prompt、数据版本可查询。

### 10.3 安全和合规

- 产品定位为研究辅助，不承诺收益；
- 不执行用户交易；
- 不将推测包装成确定性投资建议；
- 遵守数据源访问和再分发条款；
- 原始资料和用户数据隔离保存。

## 11. MVP 验收标准

选定一家港股公司后，用户可以：

1. 用一句话创建研究任务；
2. 看到可编辑研究计划；
3. 看到任务执行进度；
4. 获取一份完整研究报告；
5. 点击关键数字查看来源；
6. 查看至少一种估值计算过程；
7. 追问任意结论并得到基于证据的回答；
8. 模拟任务失败后恢复执行；
9. 对报告提出偏好反馈；
10. 下一次研究中看到该偏好生效。

## 12. 迭代计划

### Iteration 1：事实问答

单公司、财报和公告检索、数字抽取、引用展示。

### Iteration 2：基础研究报告

增加业务、财务、风险、基础估值和报告页面。

### Iteration 3：长任务运行

增加计划、异步执行、状态、checkpoint、重试和恢复。

### Iteration 4：审计和评测

增加数字校验、引用验证、时间泄漏检测、反方证据和 Benchmark。

### Iteration 5：个性化和增量更新

增加用户偏好、研究档案、新财报更新和投资逻辑变化。

## 13. 成功指标

第一版不以“预测收益率”为指标，而以研究质量和任务可靠性为指标：

- 财务数字准确率；
- 关键结论引用支持率；
- 时间泄漏率；
- 估值可复算率；
- 任务完成率；
- 中断恢复成功率；
- 用户重复纠正率；
- 单次研究耗时和成本。

