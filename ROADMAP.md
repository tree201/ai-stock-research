# AI Stock Research Agent：完整开发计划

**版本：** v1.0 规划

**当前基线：** 本地可运行 MVP，支持样例资料、规则抽取、财务计算、三情景 DCF、DeepSeek 接入、简单 Web 页面和 SQLite 任务存储。

## 1. 最终目标

构建一个面向认真个人投资者和独立研究员的专业级港股研究工作台：

```text
用户提出研究问题
  → Agent 制定研究计划
  → 获取真实披露和行情资料
  → 抽取有来源的结构化事实
  → 运行可复算财务模型
  → 生成带证据和反方观点的报告
  → 用户追问和修改假设
  → 新信息出现后增量更新
  → 系统逐渐适应用户的研究偏好
```

产品不是预测股价的系统，不承诺收益，不自动交易，也不把模型输出包装成确定性投资建议。

## 2. 产品定位和差异化

### 2.1 定位

> 不是帮用户写一份研报，而是帮用户持续维护一套可验证的投资研究。

### 2.2 与通用 Agent 的差异

通用 Agent 主要解决一次性报告生成；本系统重点解决：

- 长期研究项目；
- 截止日期和时间一致性；
- 结论—证据—数据—计算链；
- 可复算估值；
- 失败可恢复的长任务；
- 研究报告增量更新；
- 用户研究偏好适应；
- 金融领域专用评测。

## 3. 目标用户和核心场景

### 3.1 第一阶段用户

- 有一定投资基础的个人投资者；
- 学生或独立研究员；
- 长期跟踪少量港股公司的用户。

### 3.2 三条核心用户路径

#### A. 首次研究

```text
输入公司、问题、截止日期
→ 确认简短研究计划
→ 查看进度
→ 查看报告、证据和估值
```

#### B. 报告追问

```text
为什么这么判断？
查看证据
寻找反方
修改估值假设
```

追问绑定当前研究项目，不重新丢失上下文。

#### C. 增量更新

```text
更新这家公司
→ 获取截止上次报告后的新资料
→ 检查投资假设
→ 比较估值和风险变化
→ 生成差异报告
```

## 4. 当前完成情况

### 已完成

- 产品定位、PRD、技术设计和数据模型文档；
- 研究项目、研究运行和步骤状态机；
- 追加式运行事件日志；
- Yahoo 日线行情原型连接器；
- HTTPS 文档下载器和域名/大小限制；
- HTML、纯文本和 `pdftotext` PDF 解析；
- 带行号/页码和内容哈希的证据片段；
- 中英文财务事实抽取；
- CAGR、利润率、自由现金流、净现金和三情景 DCF；
- 规则化 Reviewer；
- Markdown/JSON 报告；
- 本地 Web MVP；
- SQLite 项目、运行、步骤、事件和研究产物持久化；
- Web 历史项目、运行、事件和报告读取接口；
- DeepSeek/OpenAI-compatible LLM Adapter；
- 25 个以上自动化测试和本地 Demo。

### 尚未完成

- 真实 HKEX/公司 IR 自动采集；
- 财报复杂表格和扫描件 OCR；
- 异步 Worker、实时进度和真正的长任务恢复；
- LLM 动态 Planner；
- 报告追问、情景修改和增量更新；
- 用户偏好自适应；
- 完整 Claim-Evidence 审计；
- 真实金融 Benchmark；
- 认证、限流、监控和生产部署。

## 5. 总体架构

```text
Web UI / CLI
  ↓
Application API
  ├── Project Service
  ├── Research Run Service
  ├── Report Service
  └── Feedback Service
  ↓
Research Orchestrator
  ├── Request Router
  ├── Context Builder
  ├── Planner
  ├── Research Workers
  ├── Financial Calculator
  ├── Reviewer
  └── Report Compiler
  ↓
Provider Seams
  ├── Document Provider
  ├── Market Data Provider
  ├── LLM Provider
  └── Storage Provider
  ↓
PostgreSQL + Object Storage + Search Index
```

设计原则：

- 模型、数据源、计算引擎和存储可替换；
- 原始文档不可变；
- 事实和报告拥有版本；
- 计算由代码完成；
- 模型输出必须结构化并经过校验；
- 任务状态和事件可恢复、可回放。

## 6. 分阶段实施计划

## Phase 0：本地 MVP（已完成）

### 目标

验证从证据到研究报告的最小闭环。

### 交付物

- 本地 CLI；
- 简单 Web 页面；
- 规则事实抽取；
- 计算引擎；
- DeepSeek Provider；
- SQLite 基础存储；
- 自动化测试。

### 完成标准

```text
PYTHONPATH=src python3 -m stock_research --demo
```

可以生成带财务事实、估值、证据位置和审计状态的报告。

## Phase 1：持久化和运行服务

### 目标

让 Web 端真正成为长期研究项目，而不是一次性请求。

### 工作项

1. 将 Web API 接入 SQLiteStore；
2. 持久化 Document、Evidence、Fact、Calculation、Claim、Report；
3. 增加项目列表、运行列表和报告读取接口；
4. 增加任务状态查询；
5. 增加运行事件查询；
6. 增加报告版本管理；
7. 增加应用重启后的运行恢复。

### 验收标准

- 重启服务后仍能打开历史项目；
- 报告、证据和计算结果不丢失；
- 同一项目可以创建多次研究运行；
- 历史报告不被新报告覆盖。

**当前状态：已完成本阶段的本地单进程版本。** SQLite 数据库由 `AI_STOCK_DB` 指定；Web 已提供项目、运行、事件、产物和报告读取接口。真正的异步 Worker、跨进程恢复和生产数据库迁移仍属于后续阶段。

## Phase 2：真实港股资料

### 目标

让系统使用真实公开资料，而不是合成样例。

### 工作项

1. 建立公司和证券代码映射；
2. 接入 HKEXnews 或用户指定公开 URL；
3. 接入公司 IR 文档列表；
4. 保存来源、发布时间和内容哈希；
5. 处理 PDF、HTML、表格和附件；
6. 处理港币、人民币、美元和单位换算；
7. 标记财报期间和发布日期；
8. 增加数据源失败和备用源策略；
9. 明确数据访问、存储和再分发授权。

### 数据源原则

```text
HKEXnews / 公司 IR
  > 授权结构化数据
  > 可靠财经新闻
  > 其他公开网页
```

### 验收标准

- 对至少 3 家港股公司获取真实年报或业绩公告；
- 每份文档可以追溯到原始 URL；
- 截止日期过滤有效；
- 真实 PDF 能产出带页码的证据片段；
- 失败文档不会静默产生错误内容。

## Phase 3：真实 LLM 研究流程

### 目标

让模型负责定性研究和计划，而不是只有规则抽取。

### 工作项

1. 接入 DeepSeek `deepseek-chat`；
2. 保留 OpenAI-compatible Provider；
3. 实现结构化 Planner；
4. 实现业务、行业和风险分析 Worker；
5. 实现 Context Builder；
6. 注入用户偏好、公司历史和截止日期；
7. 对模型输出做 JSON Schema 校验；
8. 限制模型只能引用已提供的 Evidence ID；
9. 记录 Provider、模型、Prompt 版本、耗时和 Token。

### 验收标准

- 同一研究任务能动态生成有限研究计划；
- 每个定性结论带 Evidence ID；
- 无效 JSON、未知证据和非法置信度会被拒绝；
- 模型失败时任务进入可解释的失败状态；
- 财务数学仍由代码完成。

## Phase 4：异步长任务

### 目标

让研究任务可以运行较长时间，并支持进度、重试、暂停和恢复。

### 工作项

1. 引入持久化任务队列；
2. 把每个研究步骤变为独立 Worker；
3. 添加幂等键；
4. 添加步骤超时和有限重试；
5. 添加 checkpoint；
6. 添加任务暂停、取消和恢复；
7. 增加 SSE 或轮询进度；
8. 服务启动时扫描未完成任务；
9. 防止重复下载、重复计算和重复报告。

### 验收标准

模拟以下情况后，任务仍能完成：

- 文档源超时；
- 模型请求失败；
- Worker 进程退出；
- Web 服务重启；
- 用户在执行中修改研究方向。

## Phase 5：研究工作区和追问

### 目标

让用户真正围绕研究项目持续交互。

### 工作项

1. 项目首页；
2. 任务历史；
3. 报告版本对比；
4. 点击结论查看证据；
5. “解释原因”；
6. “寻找反方”；
7. “修改估值假设”；
8. “更新研究”；
9. 阶段性结果和任务进度；
10. 失败步骤重试按钮。

### 交互原则

```text
聊天表达意图
研究任务负责执行
报告负责沉淀
证据面板负责信任
模型面板负责假设探索
```

### 验收标准

- 用户不需要选择 Agent；
- 简单问题快速回答；
- 深度问题创建研究任务；
- 追问复用当前报告上下文；
- 修改假设后只重新计算相关结果。

## Phase 6：增量更新和研究记忆

### 目标

让产品从“一次性报告”变成“长期研究助手”。

### 工作项

1. 保存公司长期研究档案；
2. 保存历史投资假设；
3. 检测新财报和重大公告；
4. 生成新旧报告差异；
5. 检查假设是否成立；
6. 记录结论被削弱或推翻的原因；
7. 建立后续跟踪指标和失效条件；
8. 增加 Watchlist。

### 验收标准

用户说“更新腾讯研究”后，系统能够只获取上次报告之后的新资料，并输出：

- 新增事实；
- 假设变化；
- 财务变化；
- 估值变化；
- 风险变化；
- 总体结论是否变化。

## Phase 7：用户自适应

### 目标

让 Agent 越用越适合用户，但保持可解释和可回滚。

### 工作项

1. 记录用户明确反馈；
2. 将反馈提取为候选偏好；
3. 用户确认后启用偏好；
4. Context Builder 注入偏好；
5. Planner 根据偏好调整研究重点；
6. 保存偏好版本；
7. 支持查看、修改和撤销。

### 第一版只学习

- 关注指标；
- 报告顺序；
- 估值风格；
- 证据优先级；
- 风险展示方式；
- 是否默认展示反方证据。

### 不自动学习

- 财务公式；
- 数据校验规则；
- 交易决策；
- 用户真实风险承受能力；
- 是否因为股票上涨而判断自己正确。

## Phase 8：评测和可靠性

### 目标

用数据证明 Agent 是否真的变好了。

### 评测层次

#### 基础金融 QA

- FinanceBench；
- FinQA；
- TAT-QA；
- 自建港股财报问答。

#### 时间一致性

- 截止日期过滤；
- 发布日期和报告期间区分；
- 历史报告重放；
- 未来信息泄漏检测。

#### 端到端 Agent

- 研究计划完整度；
- 数字准确率；
- 计算准确率；
- 引用支持率；
- 关键风险遗漏率；
- 任务完成率；
- 中断恢复成功率；
- Token、成本和耗时。

### 质量门禁

每次更换模型、Prompt、数据源或研究流程，都必须运行：

```text
单元测试
→ 数据层测试
→ 结构化输出测试
→ 时间泄漏测试
→ 端到端报告测试
→ 历史任务回放
```

## Phase 9：生产化

### 工作项

- PostgreSQL；
- 对象存储；
- 全文/向量检索；
- 用户认证；
- 多租户隔离；
- API 限流；
- 密钥管理；
- 任务成本上限；
- 结构化日志；
- 指标和告警；
- Docker 部署；
- 数据源授权审查；
- 隐私和删除机制；
- 研究免责声明和审计留痕。

## 7. 推荐实现顺序

不要一次性实现所有 Agent。采用纵向切片：

```text
真实文档
→ 真实证据
→ 真实事实
→ 真实模型分析
→ 真实报告
→ 真实持久化
→ 真实追问
→ 真实增量更新
```

具体顺序：

1. Web 接入 SQLite；
2. 文档、事实和报告完整持久化；
3. 接入一家公司的真实年报；
4. 接入真实 DeepSeek 分析；
5. 增加 Claim-Evidence 审计；
6. 增加异步任务和恢复；
7. 增加报告追问；
8. 增加增量更新；
9. 增加用户偏好；
10. 增加 Benchmark；
11. 最后做部署和多用户。

## 8. 技术决策原则

- 第一版不依赖 DSH，但借鉴事件日志、动态上下文、生命周期和可回放思想；
- 第一版不绑定单一模型；
- 第一版不绑定单一金融数据供应商；
- 规则和代码优先于模型猜测；
- 证据优先于语言流畅度；
- 可恢复优先于一次性速度；
- 小范围真实数据优先于全市场伪覆盖；
- 每个功能都必须有验收测试和失败案例。

## 9. 最终完成标准

当以下流程在真实港股公司上稳定运行时，项目达到第一版产品目标：

```text
用户输入：
“截至指定日期，研究某港股公司是否适合长期持有。”

系统完成：
真实资料获取
→ 研究计划
→ 财务事实
→ 业务/行业/风险分析
→ 可复算估值
→ 证据审计
→ 研究报告
→ 用户追问
→ 增量更新
```

并且能够证明：

- 关键数字有来源；
- 计算可以复核；
- 没有使用截止日期之后的信息；
- 任务失败后可以恢复；
- 用户偏好可以逐渐生效；
- Agent 的改进不会破坏已有研究质量。
